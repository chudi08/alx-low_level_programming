#include "main.h"
#include <stdio.h>

/**
 * _isdigit - A function that checks for a digit 0 - 9
 * @c: Character to check
 * Return: To return 1 if successful or 0 if not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
